-- ============================================================================
-- إصلاح: لم تكن هناك سياسة RLS تسمح لمستخدم جديد بإنشاء صف حسابه الخاص في
-- app_users أول مرة (كانت السياسات الموجودة تغطي فقط select/update/admin-all).
-- بدون هذه السياسة يفشل تدفق "طلب انضمام" بالكامل عند أول إدخال.
-- ============================================================================

create policy "مستخدم جديد ينشئ حسابه" on public.app_users
  for insert with check (auth_user_id = auth.uid());
