-- ============================================================================
-- حسابات الفريق (مدير/مراجع) — القسم 28: شاشة "المستخدمون" في لوحة الإدارة.
-- ============================================================================
-- app_users لا يملك اسمًا أو بريدًا خاصًا به (بيانات الهوية والاتصال تعيش في
-- identities/contacts، وهي مخصصة لأصحاب الملفات المسجَّلين في الدليل فقط).
-- حسابات الفريق ليست كفاءات مسجَّلة، ولا نملك مفتاح service_role للوصول إلى
-- auth.users مباشرة من التطبيق لعرض بريدها — لذا نضيف عمودين اختياريين هنا
-- فقط لغرض عرض هوية الحساب في شاشة إدارة المستخدمين.

alter table public.app_users add column if not exists staff_name text;
alter table public.app_users add column if not exists staff_email text;

-- ملاحظة: لا حاجة لسياسات RLS جديدة — سياسة "الإدارة تدير الحسابات" الحالية
-- (for all using (is_admin())) تسمح للمدير أصلاً بإدراج/تعديل أي صف في
-- app_users، بما في ذلك حسابات الفريق الجديدة وتغيير أدوارها لاحقًا.
